theme: jekyll-theme-tactile
timezone: US/Pacific
permalink: /:year/:month/:day/:slug
title: The Hiking Vegan
description: Abbreviated thoughts from the trail.
defaults:
  - scope:
      path: "assets/img"
    values:
      image: true
  - scope:
      path: "assets/kml"
    values:
      kml: true
  - scope:
      path: "_posts"
      type: "posts"
      layout: "post"
  - scope:
      path: "_posts/pct"
      type: "posts"
      layout: "post"
    values:
      category: PCT
      tags: PCT